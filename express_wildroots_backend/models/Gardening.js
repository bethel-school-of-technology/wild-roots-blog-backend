//require mongoose
var mongoose = require('mongoose');
const { Schema } = mongoose;

var Gardening = mongoose.Schema;

// Define schema
var Gardening = mongoose.Schema;

var Gardening = new Schema({
   
});


// Compile model from schema
var GardeningModel = mongoose.model('Gardening', Gardening );

module.exports = GardeningModel;